#!/bin/sh
# Pre-commit hook for WebPrinter

echo "🔍 Pre-commit 검증 실행 중..."

# Node.js 환경에서 pre-commit 스크립트 실행
npm run pre-commit

# 스크립트 실행 결과에 따라 커밋 허용/차단
exit $?